import React from 'react';
import ReactDOM from 'react-dom/client';
import BTGlasses from './BT_glasses/BTGlasses';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <BTGlasses/>
);
